// js/mobile.js

export function init() {
  console.log('[mobile.js] Iniciando modo MOBILE');
  // Coloca aqui a UI/controles mobile (touch, etc) pra mídia 360.
  // Por ora é só stub pra não dar erro no import.
}

export function dispose() {
  console.log('[mobile.js] Limpando modo MOBILE');
  // Limpa listeners ou objetos criados no init(), se tiver.
}
